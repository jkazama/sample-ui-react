import { cn } from "@/lib/utils";

export const AssetCashflow = ({ className }: { className?: string }) => {
  return (
    <div
      className={cn(
        "p-3 h-[calc(100vh-80px)] text-sm text-gray-400",
        className
      )}
    >
      This page is not yet implemented.
    </div>
  );
};
